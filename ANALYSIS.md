# Анализ кодовой базы PPR-Client

Дата анализа: 2026-01-25

---

## 1. Оценка архитектуры

### Сильные стороны
- **FSD (Feature-Sliced Design)** с нумерованными слоями — чёткое разделение ответственности
- **Server Actions** — современный подход Next.js 14 для мутаций
- **Явные кросс-импорты** через `@x/` — хорошая практика контроля зависимостей
- **Drizzle ORM** — типобезопасная работа с БД
- **React Context** для состояния — подходит для масштаба проекта

### Проблемы архитектуры

| Проблема | Файл | Влияние |
|----------|------|---------|
| **Монолитный PprProvider** | `src/2entities/ppr/ui/PprProvider.tsx` | 881 строка, 40+ методов. Нарушает SRP |
| **Бизнес-логика в entities** | `PprProvider.tsx` | Должна быть в features слое |
| **Расчёты в TSX** | `correctionRaport/`, `reports/` | Сложная логика в UI-компонентах |
| **Дублирование кода** | `calculateLaborCost.ts`, `calculateFulfillmentReport.ts` | 26 строк копипасты в каждом файле |

---

## 2. Оценка тестового покрытия

### Статистика (актуально на 2026-02-12)
| Метрика | Значение |
|---------|----------|
| Файлов с тестами | 11 unit + 2 e2e |
| Всего unit-тестов | **233** |
| Тестов (было) | ~125 (2026-01-28) |

### Что покрыто
- Утилиты `math`, `date` — хорошие параметризованные тесты
- `authConfig` — с моками
- `createPprMeta` — с фикстурами
- `WorksService`, `WorkingMenService` — полное покрытие сервисов
- **`PprYearPlanStatusUpdater`, `PprMonthPlanStatusUpdater`** — machine states + role matrix ✓
- **`isPprInUserControl`** — авторизационная логика ✓
- **`calculateFulfillmentReport`, `calculateLaborCost`** — расчёты отчётов ✓
- **`checkIsTimePeriodAvailableToTransfer`** — валидация переносов ✓
- **`findFirstUndonePprPeriod`** — утилита периодов ✓
- E2E workflow согласования ППР

### Что НЕ покрыто (критично)
- **Server Actions** (`ppr.actions.ts` — 707 строк) — требует интеграционных тестов с БД
- **React-компоненты** (Testing Library установлен, но не используется)
- **XLSX экспорт** (`pprConvertToXlsx.ts`)

### Вердикт
**Критическая бизнес-логика теперь покрыта.** Можно вводить новые фичи с низким риском регрессий в core-модулях.

---

## 3. Критические проблемы безопасности

| Проблема | Файл | Решение |
|----------|------|---------|
| **Пароли в plaintext** | `authConfig.ts:22` | Использовать bcrypt/argon2 |
| **Логирование user данных** | `createServerActionReturn.ts` | Убрать из продакшена |
| **localhost в allowedOrigins** | `next.config.js:17` | Вынести в env-переменные |

---

## 4. Технический долг

### TODO/FIXME комментарии (20 шт.)

Ключевые:
- `PprProvider.tsx:33` — убрать null из контекста
- `PprProvider.tsx:186` — вынести в отдельную функцию расчет чел.-ч
- `PprProvider.tsx:364` — сделать единую функцию перерасчёта итогов
- `calculateFulfillmentReport.ts:13` — "перемудрил, получилось сложно"
- `calculateLaborCost.ts:141` — вынести расчёты в сервисы
- `PprMeta.ts` — разбить на подклассы, переделать архитектуру
- `PprField.ts` — сделать сервис вместо класса со static методами
- `authConfig.ts` — предусмотреть режим редактирования только одного ПК
- `workingMansTableColumnsHelpers.ts` — переделать с forEach и избавиться от копипасты
- `addYearPlanSheet.ts` — переписать на обычный цикл, перенести стилизацию

### any типы (11 шт.)

В основном в `PprField.ts` — 6 методов-валидаторов с `any` параметрами:
```typescript
static isYearPlanField(field: any): field is keyof PlannedWorkWithCorrections
static isPlanWork(field: any): field is PlanValueField
static isFactWork(field: any): field is FactValueField
// ... и другие
```

### Неконсистентности
- "Raport" vs "Report" — смешанное написание в названиях файлов и функций
- `eslint-disable-next-line` без обоснования — 3 места

### Большие файлы

| Файл | Строк | Проблема |
|------|-------|----------|
| `PprProvider.tsx` | 881 | Монолитный компонент, 40+ методов |
| `ppr.actions.ts` | 707 | Много логики в одном файле |
| `calculateFulfillmentReport.ts` | 324 | Сложная логика |
| `addYearPlanSheet.ts` | 316 | Excel-форматирование смешано с логикой |
| `createPprMeta.ts` | 285 | Создание метаданных |
| `calculateLaborCost.ts` | 260 | Сложные расчёты |

---

## 5. Задачи перед расширением проекта

### Приоритет 1 — Критические (блокируют развитие)

| # | Задача | Обоснование | Сложность |
|---|--------|-------------|-----------|
| 1.1 | **Хеширование паролей** | Уязвимость безопасности | Низкая |
| 1.2 | **Разбить PprProvider** на `WorkProvider`, `WorkingManProvider`, `MetaProvider` | 881 строка — невозможно поддерживать | Высокая |
| 1.3 | **Вынести бизнес-логику из entities в features** | Нарушение FSD, логика "размазана" | Средняя |
| 1.4 | **Убрать localhost из production config** | Безопасность | Низкая |

### Приоритет 2 — Высокий (нужно до добавления фич)

| # | Задача | Обоснование | Сложность |
|---|--------|-------------|-----------|
| 2.1 | **Добавить unit-тесты для Server Actions** | 707 строк `ppr.actions.ts` без тестов | Средняя |
| ~~2.2~~ | ~~**Покрыть тестами расчёты отчётов**~~ | ~~Сложная логика, легко сломать~~ | ✅ **Выполнено 2026-02-12** |
| 2.3 | **Убрать дублирование в calculateLaborCost/FulfillmentReport** | 52 строки копипасты | Низкая |
| 2.4 | **Заменить any на unknown/типы в PprField.ts** | 11 мест с потерей типизации | Низкая |
| 2.5 | **Вынести расчёты из TSX в сервисы** | Бизнес-логика в UI | Средняя |

### Приоритет 3 — Средний (улучшение качества)

| # | Задача | Обоснование |
|---|--------|-------------|
| 3.1 | Убрать null из PprContext (TODO в коде) | Упростит компоненты |
| 3.2 | Консистентность "Report" вместо "Raport" | Читаемость |
| 3.3 | Вынести magic numbers в константы | Excel экспорт |
| 3.4 | Добавить React component tests | Testing Library не используется |
| 3.5 | Убрать неиспользуемый Tailwind | Лишние зависимости |

### Приоритет 4 — Низкий (когда будет время)

| # | Задача |
|---|--------|
| 4.1 | Рефакторинг PprField из static-класса в сервис |
| 4.2 | Разбить mock-файлы (37k строк) |
| 4.3 | Заменить .concat() на spread operator |
| 4.4 | Документировать eslint-disable |

---

## 6. Рекомендуемый порядок работ

```
1. Безопасность (1.1, 1.4)                    ← Сделать первым
        ↓
2. Архитектурный рефакторинг (1.2, 1.3, 2.5)  ← Основная работа
        ↓
3. Тестирование (2.1, 2.2, 3.4)               ← Покрыть критичное
        ↓
4. DRY и качество кода (2.3, 2.4, 3.1-3.3)    ← Чистка
        ↓
5. Новый функционал                            ← Можно добавлять фичи
```

---

## 7. Итоговая оценка

| Аспект | Оценка | Комментарий |
|--------|--------|-------------|
| Архитектура | 7/10 | FSD хорошо, но PprProvider — монолит |
| Типизация | 6/10 | 11 any, но в целом TypeScript используется |
| Тестирование | 6/10 | 233 unit-теста, критическая логика покрыта; нет тестов Server Actions и компонентов |
| Безопасность | 5/10 | Plaintext пароли — критично |
| Maintainability | 5/10 | 20 TODO, большие файлы |

**Общая готовность к расширению: 5/10**

Перед добавлением нового функционала рекомендуется выполнить задачи приоритетов 1 и 2 — это снизит риски и упростит дальнейшую разработку.

---

## 8. История изменений

### 2026-02-12: Покрытие тестами перед новыми фичами (задачи 2.2)

**Выполнено:**
- Написано 108 новых unit-тестов для критической бизнес-логики
- Итого unit-тестов: **233** (было 125)

| Новый файл | Тесты | Что покрыто |
|------------|-------|-------------|
| `PprYearPlanStatusUpdater.test.ts` | 34 | Все переходы статусов года, матрица ролей × статус |
| `PprMonthPlanStatusUpdater.test.ts` | 25 | 11 статусов месяца, откаты, `checkIsDoneAll` |
| `isPprInUserControl.test.ts` | 23 | Авторизация по роли + id подразделения/дистанции |
| `calculateFulfillmentReport.test.ts` | 15 | Агрегация план/факт работ, фильтры, rowSpan |
| `calculateLaborCost.test.ts` | 11 | plan_time = original × norm_of_time, handCorrection |
| `checkIsTimePeriodAvailableToTransfer.test.ts` | 9 | Валидация доступности месяца для переноса |
| `findFirstUndonePprPeriod.test.ts` | 11 | Поиск первого незакрытого периода |

**Найденный баг:**
- `calculateFulfillmentReport.ts:241` — `tempRowSpan` инициализируется `1`, затем инкрементируется при первом элементе. В результате `rowSpan` на 1 больше реального числа строк в блоке (off-by-one).

**Задачи из Приоритета 2 после этого:**
- ~~2.2 Покрыть тестами расчёты отчётов~~ ✓
- 2.1 Unit-тесты для Server Actions — всё ещё не сделано

---

### 2026-01-28: Рефакторинг PprProvider (задача 1.2)

**Выполнено:**
- Разбит монолитный `PprProvider.tsx` (881 строка) на 3 контекста:
  - `PprDataContext` — хранение данных (ppr, pprMeta)
  - `WorksActionsContext` — действия над работами (12 методов)
  - `WorkingMenActionsContext` — действия над работниками (7 методов)
- Создано 2 сервиса с чистыми функциями:
  - `WorksService` — 16 методов
  - `WorkingMenService` — 7 методов
- Написано 61 unit-тест для сервисов
- Сохранена обратная совместимость через `usePpr()` фасад

**Новая структура:**
```
src/2entities/ppr/
├── lib/services/
│   ├── WorksService.ts
│   └── WorkingMenService.ts
└── ui/provider/
    ├── PprDataContext.tsx
    ├── WorksActionsContext.tsx
    ├── WorkingMenActionsContext.tsx
    ├── PprProvider.tsx
    └── usePpr.ts
```

**Результат:** Тестовое покрытие увеличено до 105 тестов (+61). Архитектура улучшена.
